<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haruto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Haruto"
        android:usesCleartextTraffic="true">
        <activity android:name=".EditDeleteEMPActivity"></activity>
        <activity android:name=".AccountEmpActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/menu_emp" />

        <activity android:name=".HomePageEmpActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".customerInfoActivity" />
        <activity android:name=".RepasswordActivity" />
        <activity android:name=".EditDeleteActivity" />
        <activity android:name=".ReserveActivity" />
        <activity android:name=".AccountCusActivity" />
        <activity android:name=".MybookingActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".EmployeeLoginActivity" />
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>